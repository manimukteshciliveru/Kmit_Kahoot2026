# -----------------------------------------------------------------------------
# PRODUCTION ENVIRONMENT CONFIGURATION
# -----------------------------------------------------------------------------
# RENAME this file to .env on your server and update values.
# -----------------------------------------------------------------------------

# --- Domain Settings ---
DOMAIN=quizmaster.com
EMAIL=admin@quizmaster.com
CLIENT_URL=https://quizmaster.com

# --- Application Settings ---
PORT=5000
NODE_ENV=production
# Generate a strong secret: openssl rand -base64 32
JWT_SECRET=change_this_to_a_very_long_random_secret_string

# --- Database Credentials (MongoDB) ---
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=change_this_to_strong_mongo_password
# Connection String (Internal Docker Network)
MONGODB_URI=mongodb://admin:change_this_to_strong_mongo_password@mongo1:27017,mongo2:27017,mongo3:27017/quizmaster?authSource=admin&replicaSet=rs0

# --- Cache Credentials (Redis) ---
REDIS_PASSWORD=change_this_to_strong_redis_password
